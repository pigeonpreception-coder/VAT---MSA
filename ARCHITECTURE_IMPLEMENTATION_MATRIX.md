# VAT-MSA Architecture Implementation Matrix

**Evidence date:** 2026-08-27
**Repository scope:** executable controlled pilot and production-oriented application foundation  
**Governing architecture:** `08-enterprise-architecture/` and its approval gate

This matrix is the truthful completion record for the approved architecture. It separates working repository capabilities from controls that require NamRA, ITAS, legal, security, provider or infrastructure authority. A disabled boundary is an implemented safety control, not a simulated external service.

## Status vocabulary

- **VERIFIED PILOT** — executable end-to-end behavior is implemented and has automated or runtime evidence.
- **CONTROLLED FOUNDATION** — the tenant-scoped model, policy, data and read/workflow surface exist, but some lifecycle commands remain phased.
- **DISABLED PENDING AUTHORITY** — an adapter or state machine exists and explicitly refuses legal/financial effect until an approved contract, credential or rule set is supplied.
- **ARCHITECTURE / PRODUCTION EVIDENCE** — design artifacts exist, but completion can only be demonstrated in the selected national production environment.

## Domain coverage

| # | Domain | Current implementation | Status | Required closure before national production |
|---:|---|---|---|---|
| 1 | Identity | Provider registry, immutable provider-subject links, provisioned-user checks, role/capability policy, development-only identity fallback, standalone `ResolveIdentity` (`GET /api/v1/identity/links`), `GetAssurance` (`GET /api/v1/identity/assurance`, combines identity-link assurance with a real server-verified step-up freshness check), `LinkIdentity` (`POST /api/v1/identity/links`, fails closed against any provider not `ACTIVE`+`CONFIGURED` — today only `SITES_WORKSPACE`) and `RevokeSession` (`POST /api/v1/identity/links/:id/revocation`) — both now tenant-scope-checked on the target user, closing a 2026-08-27 account-takeover finding (`SECURITY_GAP_ASSESSMENT.md` item #1). The same date closed item #2: real MFA — a standards-compliant RFC 6238 TOTP implementation (`lib/domain/mfa.ts`, pure Web Crypto, no external provider) backing `EnrollTotp`/`VerifyTotpEnrollment` (`POST /api/v1/identity/mfa/totp[/verification]`) and `ConfirmStepUp` (`POST /api/v1/identity/step-up`, anti-replay via a persisted HOTP counter) — replaces the previous client-asserted, unverified `x-vat-msa-auth-assurance` header every "step-up gated" command relied on | CONTROLLED FOUNDATION | Approved ITAS/enterprise IdP claims, account recovery, credential lifecycle (MFA reset/disable), PAM/JIT elevation |
| 2 | Taxpayer | Canonical taxpayers, versioned identifiers, VAT status, scoped queries, idempotent registration intake, a standalone (non-ITAS) manual-review approval path (`POST /api/v1/registration-applications/:id/decision`) that materializes the taxpayer on approval, `SuspendTaxpayer` (`POST /api/v1/taxpayers/:id/suspension`, idempotent), an `IdentifierVersion` correction path (`POST /api/v1/taxpayers/:id/identifiers/:identifierId/correction` — supersedes rather than overwrites, keeps the denormalized `taxpayers.vat_number`/`tin` columns in sync) and a standalone, re-triggerable `VerifyIdentifiers` (`POST /api/v1/taxpayers/:id/identifiers/verification`, calls the same ITAS port registration intake uses and honestly reports `AWAITING_PROVIDER_CONTRACT` while ITAS remains unconfigured). Module 10 Phase B (2026-08-27) found registration intake's own claim of "calls the same ITAS port" was previously false — `submitRegistrationApplication` hardcoded `AWAITING_PROVIDER_CONTRACT` directly and never actually called `verifyTaxpayer` — now fixed, so a registration application genuinely reflects whatever the ITAS port reports rather than an assumption baked in at intake | CONTROLLED FOUNDATION | Authoritative ITAS verification, merge/deregistration rules and signed identifier precedence |
| 3 | Organisation | One taxpayer/one organisation constraint, branches, memberships, effective-dated buyer/seller capabilities, standalone `ActivateOrganisation`+`EnableCapability` (via the registration decision above, which also creates the head-office branch and owner membership), `AssignMembership` (`POST /api/v1/organisations/:id/memberships`) and standalone `ListBranches`/branch create/update (`GET`/`POST /api/v1/organisations/:id/branches`, `PATCH .../branches/:branchId`, head office cannot be deactivated) | VERIFIED PILOT | NamRA lifecycle authority and enterprise policy enforcement evidence |
| 4 | User management | Users, memberships, roles, permissions, grants, separated privileged portal projections, a standalone `GetUserAccess` query (`GET /api/v1/me/access`) returning the full effective RBAC+ABAC predicate set computed live and never cached, `ProvisionUser` as explicit invite-and-claim (`POST /api/v1/organisations/:id/invitations` + `POST /api/v1/invitations/claim`) and standalone `SuspendUser`/reactivate (`POST /api/v1/users/:id/suspension`, `POST /api/v1/users/:id/reactivation` — a reversible lockout distinct from `terminateEmployee`'s one-way offboarding, tenant-scoped, self-suspension denied) | CONTROLLED FOUNDATION | Enterprise provisioning, real invitation delivery, PAM and periodic access certification |
| 5 | Buyer/Seller | Dynamic organisation capabilities and transaction-context roles without duplicate taxpayer identities; standalone `GetAvailablePortals` (`GET /api/v1/portals`, wrapping the existing per-page gate) and `ClassifyTransaction` (`GET /api/v1/counterparties/classification?vat_number=`), a public-posture pre-flight check reusing invoice certification's own counterparty resolution rules | VERIFIED PILOT | Legal terminology and operating-policy approval |
| 6 | Customer | Tenant-scoped create/update and non-destructive deactivate lifecycle, duplicate identifier checks, active-relationship enforcement, audit/outbox evidence and immutable transaction snapshots. Module 5 Phase A added `SearchCustomers` (`GET /api/v1/business-parties?relationship=CUSTOMER`, filterable by `q`/`status`, bounded/paginated with a real `total_count` — Module 3 Phase B's `GetWorkQueue` shape) — the endpoint was previously a fixed, unfiltered 100-row list. There is still no separate structured `Contact`/`Address` sub-entity: `business_parties` keeps them as flat `email`/`phone`/`address` fields, a deliberate scope decision (documented in `lib/domain/business.ts`) given no other evidence for a structured multi-contact model exists anywhere in this schema | VERIFIED PILOT | Authoritative customer lookup/verification contract, conformance evidence, and a structured `Contact`/`Address` model if genuinely required |
| 7 | Supplier | Tenant-scoped create/update and non-destructive deactivate lifecycle, VAT/TIN snapshots, duplicate identifier checks and active-relationship enforcement for new expenses. Module 5 Phase A closed the two gaps a 2026-08-25 code audit found: `SearchSuppliers` (the same search as Customer's, filtered `relationship=SUPPLIER`) and `VerifySupplier` (`POST /api/v1/business-parties/:id/verification`), which previously did not exist anywhere in the codebase. Per the playbook's own instruction, this is explicitly "against Module 1's taxpayer adapter, not a fresh identity concept" — it calls `classifyTransaction` (`lib/data/identity-repository.ts`), the exact same cross-tenant VAT-number resolution invoice certification and Module 1's own `ClassifyTransaction` pre-flight check already use. A new `party_verification_snapshots` table gives the playbook's named `VerificationSnapshot` real persistence: every verification writes a fresh row rather than returning a cached result — the same "always re-check live" departure Module 4's `evaluateRisk` already established, since "is this supplier valid as of today" cannot be answered from stale data | VERIFIED PILOT | Authoritative supplier lookup/verification adapter and conformance evidence (the underlying `classifyTransaction` check is itself pilot-scope, not yet backed by a live ITAS contract) |
| 8 | Quotation | Server-calculated issue, hash-chained immutable edit revisions, rejection reasons, explicit overdue expiry, acceptance guards and recoverable idempotent conversion to a certified invoice. Module 5 Phase B retrofitted a genuine `DRAFT` initial status: `CreateQuotation` previously landed directly in `ISSUED` with no draft state at all, despite the playbook's own action list ("Create/Edit/Send/Accept/Reject/Expire/Convert") implying one. `SendQuotation` (`POST /api/v1/quotations/:id/sending`) is the new explicit `DRAFT → ISSUED` transition; `EditQuotation` now works from either `DRAFT` or `ISSUED` (previously `ISSUED` only), still refusing an edit once accepted/rejected/expired/converted. Every ACCEPT/REJECT/EXPIRE/CONVERT transition rule from before this phase is unchanged. `SearchQuotes` (`GET /api/v1/quotations`, filterable by `status`/`customer_party_id`/`q`) replaces the previous unfiltered fixed list, bounded/paginated with a real `total_count` — Module 5 Phase A's `SearchCustomers`/`SearchSuppliers` shape. Deliberately NOT built: an `Approval` sub-entity — `quotations.approved_by` remains an unwritten column, since the playbook's Phase B action list names no "Approve" verb for quotations (unlike Expense/Project's explicit approval verbs in Phase E) | VERIFIED PILOT | Configurable approval-threshold contract and UAT |
| 9 | Tax invoice | Submission, validation, duplicate/idempotency controls, certification, public verification, credit/debit correction lineage and certificates. Every line is now additionally checked against domain #10's rule catalogue at certification time — see that row. Idempotency is now safe under genuinely concurrent retries, not just sequential ones: a race on the SELECT-then-INSERT idempotency check is caught and recovered into the same idempotent response, rather than surfacing an unhandled UNIQUE-constraint error (proven under a real interleaved-async concurrency test, not just a unit test). The unidentified-buyer guarantee (no INPUT_VAT posting for an unresolved buyer) — previously correct but completely untested, flagged P0 in MODULE_DEVELOPMENT_PLAYBOOK.md — now has direct route-level coverage for both the suppressed and the posted case. `invoice_number` is now unique per supplier (`UNIQUE(supplier_taxpayer_id, invoice_number)`, plus an explicit pre-check for a clear conflict message) — previously unenforced. `CancelInvoice` (`POST /api/v1/invoices/:id/cancellation`) now exists: officer-only (`invoices:cancel`, held only by PILOT_ADMIN/NAMRA_SYSTEM_ADMIN, never the submitting taxpayer) and step-up gated, restricted to original tax invoices with no active correction against them, reversing ledger/return effect the same way a credit note does rather than deleting anything. `VerifyInvoice`'s public output now surfaces correction and cancellation lineage (masked invoice numbers, no reason text). A `vat_transactions` table now formalizes every certification, correction and cancellation as its own linked record (`PostTransaction`/`Reverse`, previously only an implicit `transaction_id` tag on `ledger_entries`), and standalone `GetTransactionTimeline` (`GET /api/v1/invoices/:id/transaction-timeline`) reads back the complete chronological audit narrative for one invoice's lineage — reachable from any invoice id within it (the original or any correction), always resolving to the same root | VERIFIED PILOT | Legal particulars, an official numbering/reservation *allocation* system for offline/high-volume devices (today's per-supplier uniqueness constraint prevents collisions but doesn't pre-allocate ranges), formal NamRA sign-off on the cancellation eligibility policy implemented, and HSM signature profile |
| 10 | VAT | A real, NamRA-owned rule catalogue (`vat_rules`, effective-dated, `UNIQUE(tax_category,country,version)`), with `ProposeVatRule`/`ApproveVatRule` (`POST /api/v1/vat-rules`, `POST /api/v1/vat-rules/:id/approval` — segregation of duties: the proposing officer cannot approve their own draft), standalone `EvaluateVAT` (`GET /api/v1/vat-rules/evaluate?tax_category=&date=`) and `ExplainCalculation` (`GET /api/v1/invoices/:id/vat-explanation`, per-line traceability to the exact approved rule version). Deployment-bootstrapped with Namibia's actual statutory rates (15% standard; 0% zero-rated/exempt/outside-scope; reverse-charge at the standard rate) — `OTHER` is deliberately left with no approved rule, so any line categorized `OTHER` fails closed rather than falling back to a default. Wired into invoice certification (`submitInvoice`): a line whose supplied rate doesn't match the resolved rule, or whose category has no approved rule at all, is rejected before any row is written — previously the tax rate was entirely client-supplied and only checked for internal arithmetic consistency, never against any server-owned rate | VERIFIED PILOT | Golden cases, a formal rate-change approval/rollback runbook, and NamRA sign-off on the bootstrapped rate catalogue itself |
| 11 | VAT transaction | Atomic invoice/certificate/VAT ledger writes, signed correction effects, adjustments, audit and outbox | VERIFIED PILOT | Enterprise ledger reconciliation and target-load recovery evidence |
| 12 | Accounting | A 2026-08-25 code audit found this row's prior "VERIFIED PILOT" claim materially overstated: only `PostJournal` existed (no `Account` create, no `AccountingPeriod` table at all, no `ReverseJournal`, no `TrialBalance`/statements). Module 5 Phase C closed that gap for real. `CreateAccount` (`POST /api/v1/accounting/accounts`) now exists, pre-checking the (organisation, code) uniqueness the table's own constraint enforces. `ReverseJournalEntry` (`POST /api/v1/accounting/journals/:id/reversal`) posts a brand-new, equal-and-opposite entry as of today rather than editing or deleting the original — the original flips to `status='REVERSED'` as a traceability marker only, and both entries' lines count toward every balance report (their opposite amounts net to zero, which is what makes it a real reversal and not a deletion in disguise); a journal can only be reversed once. A new `accounting_periods` table backs `ClosePeriod` (`POST /api/v1/accounting/periods/closure`) — periods are implicit and open until explicitly closed (no separate create-period step), idempotent on an already-closed period, and refuses to close a period that hasn't ended yet. Once closed, both `PostJournal` and `ReverseJournalEntry` refuse any posting dated inside that period. `TrialBalance` (`GET /api/v1/accounting/trial-balance`) and a deliberately simplified `Statements` endpoint (`GET /api/v1/accounting/statements`: income statement + a balance sheet that folds the same-range net income in as a computed "retained earnings" line, since no period-end closing journal automates that roll-forward — a proportionate simplification for this module's "lighter CRUD standard", not full general-ledger closing) both now exist as real read paths, gated on `accounting:read`. `ClosePeriod` requires a new `accounting:close-period` permission, held only by `PILOT_ADMIN`/`TAXPAYER_OWNER`/`TAXPAYER_ACCOUNTANT` (the same roles that already hold `accounting:post`). Proven by 6 new domain tests and 14 new route-level tests (`tests/routes/module-5-accounting.test.ts`) — this module's first-ever route-level test file; Module 5 previously had zero | VERIFIED PILOT | AR/AP, period-end closing journals to actually roll revenue/expense into equity, and approved financial-statement presentation rules |
| 13 | Inventory | A 2026-08-25 code audit found `products`/`warehouses` were seed-only (2 products, 1 warehouse, hardcoded — no application code ever wrote to either table) despite this row's prior "VERIFIED PILOT" claim. Module 5 Phase D built `CreateProduct` (`POST /api/v1/products`) and `CreateWarehouse` (`POST /api/v1/warehouses`), mirroring Phase C's `CreateAccount`/Phase E's `CreateExpenseCategory` fix for their own seed-only tables — both reject a duplicate SKU/code and, for `CreateWarehouse`, an unowned `branch_id` reference. `TransferStock` (`POST /api/v1/inventory/transfers`) closes the one real atomicity gap: previously a "transfer" needed a caller to issue two separate, unlinked `RecordStockMovement` calls with nothing tying them together; now both legs post in one `db.batch` sharing a transfer id, and the destination leg's cost is read from the source warehouse's own current weighted-average cost rather than caller-supplied, since a transfer moves the same physical stock and must preserve cost basis. `GetAvailability`/`Valuation` (`GET /api/v1/inventory/availability`, `.../valuation`) are new aggregated reads — the pre-existing `balances` view only ever surfaced a raw per-(warehouse,product) list with no per-product total or organisation-wide value; these compute both, valued at each balance's own weighted-average cost. Building `TransferStock` also surfaced and fixed a genuine pre-existing bug in `RecordStockMovement`'s balance upsert: its `INSERT ... ON CONFLICT DO UPDATE` crashed with a false `CHECK constraint failed: quantity_micros >= 0` on any negative-delta write against an existing balance row (reproduced in isolation against the same SQLite engine this suite runs on — the engine evaluates the CHECK against the `excluded` pseudo-row's raw pre-update values, not the resolved post-update row) — a bug that had simply never been exercised by any test before `TransferStock`'s second-write-to-an-existing-balance path. Fixed by computing the new quantity/average cost in application code and issuing a plain `INSERT` or `UPDATE`, shared by both commands. Deliberately NOT built: distinct `Receive`/`Issue`/`Adjust` command names — `RecordStockMovement`'s existing `movement_type` parameter (`RECEIPT`/`ISSUE`/`ADJUSTMENT_IN`/`ADJUSTMENT_OUT`) already fully and safely covers that semantic split with a validated enum and automatic sign handling; adding three more near-duplicate commands would be pure API-surface bloat against this module's own "resist scope creep" watch-out, not a closed capability gap | VERIFIED PILOT | Valuation policy (today's weighted-average cost is a computed convention, not an approved accounting policy), cycle counts and alert operations |
| 14 | Expense | A 2026-08-25 code audit found this row's prior "Maker-checker approval/rejection" closure item was actually a complete absence, not a partial gap: `status`/`approved_by`/`approved_at` were vestigial columns no command ever wrote past `DRAFT`. Module 5 Phase E built the real workflow. `CreateExpenseCategory` (`POST /api/v1/expenses/categories`) replaces the previously seed-only `expense_categories`, mirroring Phase C's `CreateAccount` fix for `chart_of_accounts`. `SubmitExpense`/`ApproveExpense`/`RejectExpense` (`POST /api/v1/expenses/:id/submission`, `.../approval`, `.../rejection`) move an expense `DRAFT → SUBMITTED → APPROVED`/`REJECTED`; a new `rejection_reason` column records why. Maker-checker separation denies the expense's own creator from approving or rejecting it — the same self-review rule Module 3's `reviewRefund` already established, enforced by an actor check rather than a new permission tier, matching this module's "lighter CRUD standard." `ExpenseReport` (`GET /api/v1/expenses/report`) now exists too: totals by status and by category over a date range, plus matching line items. Deliberately NOT built: an Expense-scoped `Budget` entity — no `expense_budgets` table exists anywhere in the schema, unlike `Category`, which reused an existing table; inventing one now would mean designing an entire feature area with no other evidence for it in this codebase, out of proportion for this phase | VERIFIED PILOT | Expense-scoped budgets (if genuinely needed — no schema evidence for the concept exists today) and accounting posting policy (an approved expense does not auto-post a journal entry) |
| 15 | Project | A 2026-08-25 code audit found this row's prior "Budget approval and governed cost/revenue posting lifecycle" closure item was actually a complete absence: `project_budgets` rows were permanently stuck `PROPOSED` (no command ever approved one) and `project_costs` was never written by any application code path (seed-only). Module 5 Phase E built the real workflow. `ApproveBudget` (`POST /api/v1/projects/:id/budget-approval`) acts on the project's one `'TOTAL'` budget row — the only category `CreateProject` ever inserts — with an approved amount independent of what was originally proposed; the project's own manager (set to whoever called `CreateProject`) cannot approve their own project's budget, the same maker-checker rule Expense uses. `PostCost` (`POST /api/v1/projects/:id/costs`) reuses `project_costs`' own `UNIQUE(project_id, cost_type, source_id)` constraint as its design: an `EXPENSE`-type cost cites an already-`APPROVED` expense tagged to the project, with amount/currency/date derived from that expense — never re-entered — so the same expense can never be posted as a cost twice; a `MANUAL`-type cost is for expenditure this system has no other record of, with the caller supplying everything (`project_costs` gained a `description` column for this). `ProfitabilityReport` (`GET /api/v1/projects/:id/profitability`) is genuinely new, not just cost-vs-budget: revenue is summed from Phase C's own `journal_lines`, which already carry `project_id` on `REVENUE`-type postings — reusing that accounting infrastructure rather than inventing a second revenue concept | VERIFIED PILOT | Multi-category budgets (today only the single `'TOTAL'` category from `CreateProject` is ever created or approvable) and formal revenue-recognition policy for the `ProfitabilityReport`'s live-computed figure |
| 16 | Payment | Module 9 Phase D (2026-08-27) built the payment-instruction models and connector this row previously only described in the abstract. A typed `PaymentConnectorPort` (`lib/integrations/payment.ts`: `status`/`recordPayment`/`allocatePayment`) is backed by a genuine `SandboxPaymentConnector` mock (deterministic provider reference/response hash, real `INITIATED`→`SETTLED` status transitions), but every mutating call re-reads a real, DB-backed guard — `service_components` (`component_key='PAYMENT_CONNECTOR'`, seeded `REQUIRES_AUTHORITY_CONTRACT`/`DISABLED`, mirroring `component-itas`'s exact shape) — on *every single call*, never a cached flag, and refuses unless it reads back exactly `SANDBOX_CONFIGURED`/`SANDBOX_ACTIVE`. This is the first genuine runtime *enforcement* use of `service_components` anywhere in this codebase (every other read of it, `lib/data/platform-repository.ts`, is display-only). `RecordPayment`/`AllocatePayment` (`POST /api/v1/refunds/:id/payment`, `.../payment/allocation`) operate on a refund claim already at `PAYMENT_PENDING` (Module 9's own claim state machine, unchanged by this phase) and, on the connector's always-taken refusal path today, still record an honest `AWAITING_AUTHORITY` audit entry rather than a bare failure — mirroring `verifyTaxpayerIdentifiers`'s ITAS fail-closed shape. `GetOutstanding` (`GET /api/v1/payments/outstanding`, national-scope only) reads the honest queue of claims awaiting payment. Proven in `tests/routes/module-9-payment-connector.test.ts`: every test but one drives the real command path end to end and asserts `payment_instructions` stays empty even under repeated retries; one clearly-labelled `SIMULATION ONLY` test flips `service_components` directly in the fake D1 (something no real command anywhere can do) purely to prove the mock's own logic is sound, then restores the disabled default. `Allocation`/`SettlementReference` are modelled as `payment_instructions.status`/`provider_reference`/`settled_at`, not a separate table — one instruction has exactly one settlement today, so a join table would be structure with no behaviour behind it | DISABLED PENDING AUTHORITY | Regulated connector contracts, settlement authority (the sandbox guard's `SANDBOX_CONFIGURED`/`SANDBOX_ACTIVE` state has no command anywhere that can set it — that authorisation decision is deliberately out of engineering's scope), allocation rules beyond the current one-instruction-one-settlement model, and finance segregation of duties |
| 17 | Reconciliation | `RunMatch` (`POST /api/v1/invoices/:id/match`) now exists as a real, independent verification pass: it re-derives what an invoice's ledger postings *should* be from its own declared figures and status, and compares against what was actually posted — catching drift a bug or manual tampering could introduce, not just re-trusting the write path that already ran once (also re-verifies Module 2's unidentified-buyer guarantee as an ongoing control). Writes a real `reconciliation_matches` row (previously seed-only, never written by application code) and, on a mismatch, a `reconciliation_exceptions` row; idempotent via that table's own `UNIQUE(invoice_id, taxpayer_id)`. `Assign` (`POST /api/v1/exceptions/:id/assignment`) and `ResolveException` (`POST /api/v1/exceptions/:id/resolution`, idempotent) exist — `reconciliation_exceptions` gained `assigned_officer_id`/`resolved_by`/`resolution_notes` columns. A 2026-08-27 security audit found both performed no tenant-scope check at all (inconsistent with `RunMatch`'s own `requireTaxpayerScope` in the same file); fixed the same day. Deliberately invoice-scoped rather than a period-wide sweep or literal scheduled job (this Workers deployment has no cron/queue infrastructure configured yet) — a documented simplification, not a gap disguised as done. `GetWorkQueue` (`GET /api/v1/exceptions`) now exists too: real filter/status/officer/age predicates plus bounded pagination with a genuine `totalCount`, backed by two covering indexes designed in up front rather than retrofitted, tenant-scoped the same way the pre-existing `listExceptions` was | CONTROLLED FOUNDATION | A period-wide or scheduled `RunMatch` trigger, authoritative tolerances and operational sampling evidence |
| 18 | VAT return | Period snapshots, reproducible versioned returns, adjustments, maker-checker approval/rejection and immutable submission attempts. A legacy, parallel VAT-position computation (a `vat_returns` table, accumulated inline on every invoice submission/cancellation with no adjustments or status filtering, diverging from this row's real system) was retired 2026-08-25 — it was already dead in the UI (its only reader had zero callers) but was still actively written, a genuine duplicate-computation hazard now closed. Module 10 Phase B (2026-08-27) found and fixed `SubmitReturn` (`POST /api/v1/vat-returns/:id/submissions`) only ever checking the ITAS port's `.status()` — its own `submitVatReturn` method was dead code, never actually called, so swapping in a working adapter would still never have submitted anything. Now genuinely calls it, with the same fail-closed shape used elsewhere, and a real `ACKNOWLEDGED`/`REJECTED_BY_PROVIDER` outcome (only reachable once ITAS is authorised) alongside the existing `BLOCKED_CONFIGURATION`. The same phase found and fixed a genuine unhandled-error gap: retrying a blocked submission under a fresh idempotency key hit `vat_return_submissions`'s `UNIQUE(provider, request_reference)` as a raw 500 — now updates the same row in place via the schema's own `attempt_count` column instead | VERIFIED PILOT / DISABLED FILING | Official return formulas, ITAS payload/acknowledgement contract and amendment authority |
| 19 | Compliance | Real, solid maker-checker commands exist for case/dispute/refund handling (`openAuditCase`, `fileDispute`, `requestRefund` — correctly requires a negative net position and a `FILED` return version, else blocks with `BLOCKED_RETURN_NOT_FILED`), all idempotent and audited. Module 9 Phase A (2026-08-27) replaced the refund claim's previous 6-flat-status, no-transition-guard `reviewRefund` with a real adjacency-list state machine (`REFUND_CLAIM_TRANSITIONS` in `lib/domain/compliance.ts`, mirroring Module 4's `CASE_TRANSITIONS`/`transitionCase` pattern): `RECEIVED → RISK_REVIEW → OFFICER_REVIEW → PAYMENT_AUTHORISATION → PAYMENT_PENDING`, with `EVIDENCE_REQUESTED`/`ON_HOLD` as genuine pausable states sharing one dynamically-resolved `resume_status` column (RESUME's target is derived from the DB, never hard-coded), and `REJECTED → DISPUTED → (RISK_REVIEW | CLOSED)` for the taxpayer's own dispute path. `TransitionRefundClaim` (`POST /api/v1/refunds/:id/transition`, officer-only, self-review denied — no override path, unlike Module 4's cases) and `DisputeRefund` (`POST /api/v1/refunds/:id/disputes`, the one taxpayer-initiated action, original-requester-only) replace the old single `reviewRefund`. `PAYMENT_AUTHORISATION`'s `APPROVE` also computes a real statutory debt offset — a live `SUM(amount_cents)` query against `tax_obligations` for the taxpayer's `PENDING` obligations, never cached — and records `offset_amount_cents`/`net_payable_cents` on the claim, folded into that same transition's one audit/outbox row (the "one audit row per command" hash-chain-linearity constraint from Module 4). `PAYMENT_PENDING` is a deliberate terminal boundary: no `payment_instructions` row is ever written, no `Paid`/`Failed`/`Reversed` status exists, and no Module 8 Workflow-engine integration was added — Payment stays `DISABLED PENDING AUTHORITY` (Module 9 Phase D), and risk-engine/maker-checker routing integration is Phase C's separate job. Module 9 Phase B (2026-08-27) added a genuine claim-time freeze and an explainable check battery: `requestRefund` now snapshots the return version, its rule set, the taxpayer's reconciliation-exception counts and a tamper-evident hash of the exact invoices grounding the claim's period into `claim_snapshot`/`claim_snapshot_hash` — written once and never touched again, proven immune to later mutation of the source rows by a real drift test. A new `refund_claim_checks` table persists eight named, explainable checks per claim: `ELIGIBILITY_NEGATIVE_NET_POSITION`/`ELIGIBILITY_RETURN_FILED`/`DUPLICATE_CLAIM` formalise gates `requestRefund` already enforces; `DEBT_OFFSET_PREVIEW` and `ANOMALY_CLAIM_FREQUENCY` (3rd-or-later claim within 90 days) are new, small, advisory checks in Module 4 Phase A's own fixed-catalogue shape (acting on them is Phase C's job); `IDENTITY_VERIFICATION`/`BANK_ACCOUNT_OWNERSHIP`/`SANCTIONS_SCREENING` are persisted `NOT_CONFIGURED`, not a fabricated pass — no such provider exists anywhere in this codebase. `GetRefundClaimChecks` (`GET /api/v1/refunds/:id/checks`, tenant-scoped) reads both back. Module 9 Phase C (2026-08-27) gave `risk_tier` a real behavioural consequence for the first time: `requestRefund` now reuses Module 4 Phase A's own persisted `risk_indicators` signal (an `OPEN` `HIGH`/`CRITICAL` indicator elevates the claim above its amount-based tier — no second risk engine forked, just a read of what `EvaluateRisk` already wrote), surfaced as a new `RISK_INDICATOR_SIGNAL` check in the Phase B battery. `transitionRefundClaim` now enforces genuine two-distinct-actor maker-checker: the same officer may never approve `PAYMENT_AUTHORISATION → PAYMENT_PENDING` (the material, fund-releasing outcome) as whoever approved the step immediately before it, checked universally regardless of tier; a `HIGH`/`CRITICAL`-tier claim additionally requires a distinct officer at *every* stage (the "enhanced lane" risk-tier routing), read directly off the existing `refund_claim_transitions` log rather than migrating onto Module 8's Workflow engine, which would be a large structural change for no functional gain here. `CreateObligation` (`POST /api/v1/obligations`) and `MarkSatisfied` (`POST /api/v1/obligations/:id/satisfaction`, idempotent) also exist — `tax_obligations` was previously seed-only, never written by application code. Both are national-scope-only (statutory obligations are imposed by NamRA, not self-declared) and follow the same idempotency-key + maker-checker conventions as the rest of this file. `GET /api/v1/compliance` is a broad read-only workspace aggregate (obligations, cases, findings, disputes, risks, refunds, refund claim transitions, communications, notifications, consents, delegations) and now reflects live obligation data. This domain remains closer to an audit/dispute/refund/obligation centre than a dedicated calendar/deadline tracker — no automatic deadline escalation or reminder scheduling exists (this Workers deployment has no cron infrastructure configured, the same documented gap as Module 3's `RunMatch`) | CONTROLLED FOUNDATION | Automatic deadline escalation/reminders (needs scheduled-job infrastructure), authoritative obligation feeds and approved lifecycle notifications. (The refund payment connector itself is domain #16 Payment's own row — Module 9 Phase D built it there, kept deliberately separate from this compliance/claim-state-machine domain.) |
| 20 | Audit | A real 12-state case lifecycle now exists (`lib/domain/compliance.ts`'s `CASE_TRANSITIONS`, mirroring the licence-lifecycle state-machine pattern): PROPOSED→AUTHORIZED→ASSIGNED→PLANNING→EVIDENCE_COLLECTION→ANALYSIS→TAXPAYER_RESPONSE→FINDINGS_REVIEW→DECISION→CLOSED, plus SUSPENDED/CANCELLED and REOPEN/LINK_APPEAL side-transitions off CLOSED. Previously `audit_cases.status` was a bare string set once at creation (`'OPEN'`) and never legally transitioned by any command. One consolidated `TransitionCase` (`POST /api/v1/audit-cases/:id/transition`) is now the single code path that can ever change a case's status — deliberate design choice over eight near-duplicate bespoke commands — and every transition is recorded to a new `audit_case_transitions` table (actor, reason, from/to status, time), readable via standalone `CaseTimeline` (`GET /api/v1/audit-cases/:id/timeline`, national-scope or the case's own taxpayer). `IssueFinding` (`POST /api/v1/audit-cases/:id/findings`) now exists too, restricted to the case's analytical stages (ANALYSIS/TAXPAYER_RESPONSE/FINDINGS_REVIEW) with duplicate-`finding_code` rejection; `CLOSE` is blocked with no findings on record. `ASSIGN` validates the target officer exists and is `ACTIVE`. `OpenAuditCase` no longer auto-assigns the opener as `assigned_officer_id` — cases now open in `PROPOSED` with assignment handled entirely by `TransitionCase`'s `ASSIGN` action, separating "who opened this" from "who owns it." All of the above is national-scope-only, idempotency-keyed and audited, matching this file's established maker-checker conventions. Deliberately NOT built in Phase C: any segregation-of-duties check (same actor closing/issuing a finding on a case they opened) — reserved for its own dedicated phase. A second case-creation path now exists alongside `OpenAuditCase`: `ApproveAction` (`POST /api/v1/risk-indicators/:id/decision`, domain #21 Risk) can also create a case, but only via its own atomic escalation path — see that row. **Phase D** added a first-class, immutable evidence sub-model: `AddEvidence` (`POST /api/v1/audit-cases/:id/evidence`) cites either a canonical system record this codebase already hashes (an invoice's `payload_hash`, a VAT return version's `ledger_snapshot_hash`) or an already-uploaded, clean-scanned document (Module 22's `document_metadata`, reused rather than duplicated) or an officer-attested external record with a supplied checksum. A row is never edited in place — a correction supersedes via `supersedes_evidence_id`, and a partial unique index (`WHERE status='PRESERVED'`) guarantees only one active citation per (case, source) at a time. `RecordEvidenceCustodyEvent` (`POST /api/v1/audit-evidence/:id/custody-events`) handles `VERIFY` (re-derives the cited record's current hash and compares — a real tamper/drift detector, recorded not enforced), `SET_LEGAL_HOLD`/`RELEASE_LEGAL_HOLD` (cascading to the underlying document's own `legal_hold` column when document-backed). `GetCaseEvidence` (`GET /api/v1/audit-cases/:id/evidence`) reads the register plus full custody history. Append-only case notes also now exist (`AddCaseNote`/`GetCaseNotes`, `POST`/`GET /api/v1/audit-cases/:id/notes`) — a correction is a new note carrying `supersedes_note_id`; no note is ever updated or deleted. **Phase E** closed the segregation-of-duties gap Phase C deliberately deferred: `enforceSegregationOfDuties` (`lib/data/compliance-repository.ts`) blocks a case's own opener (`audit_cases.opened_by`, set identically whether the case came from `OpenAuditCase` or a risk-driven `ApproveAction` escalation) from also issuing a finding on it or closing it. The block has a logged exceptional-oversight override, not a dead end: supplying `override_reason` on the command lets the same actor proceed, but only if they hold the new `cases:override-sod` permission (granted only to `PILOT_ADMIN`/`NAMRA_SUPERVISOR`, never the ordinary officer roles being segregated from) — an override with the permission but no reason, or a reason without the permission, is still refused. Every override is logged on the command's own single `audit_events` row under a distinctly-named action (`AUDIT_CASE_CLOSE_SOD_OVERRIDE` / `AUDIT_FINDING_ISSUED_SOD_OVERRIDE`) carrying who opened the case, who overrode it and why — deliberately one row, not two, since the codebase's audit-hash-chaining helper isn't batch-safe against being called twice for one command | CONTROLLED FOUNDATION | External evidence-admissibility legal sign-off and officer-authority sign-off |
| 21 | Risk | `EvaluateRisk` (`POST /api/v1/taxpayers/:id/risk-evaluation`) now exists: a small, fixed, code-versioned rule catalogue (`RISK-PILOT-2026.2`) that raises NamRA-restricted `RiskIndicator` rows from patterns across evidence this codebase already computes elsewhere — `HIGH_VALUE_INVOICE_PATTERN` reuses Module 2's own per-invoice `scoreInvoice` risk level, `RECONCILIATION_EXCEPTION_BACKLOG` reuses Module 3's reconciliation exception queue, `OBLIGATION_OVERDUE` reuses Module 3 Phase D's tax obligations — rather than re-deriving any of them independently. The response always returns every rule's explainable factor (code, fired/not, score, severity, rationale), never a bare score, satisfying the playbook's explicit requirement. Indicator writes are naturally idempotent at the row level (same rule+subject+version always resolves to one row, refreshed not duplicated) and an indicator that has moved past `OPEN` into the Phase B human-review lifecycle is left untouched by further evaluation — evaluation never overwrites a human-owned decision in progress. `GetRestrictedRisk` (`GET /api/v1/risk-indicators`, filterable by `taxpayer_id`/`status`/`severity`, paginated) also now exists, gated on `risk:read` and refusing any non-national-scope actor outright — unlike `CaseTimeline`, a taxpayer cannot read their own risk indicators, matching the data dictionary's `RESTRICTED_RISK` classification and the NamRA portal's own stated policy. Two deliberate scope decisions, both documented in `lib/domain/compliance.ts`: the domain catalog's "ModelVersion" is treated as the code-versioned `rule_version` string, not a separate governed/maker-checker database table (unlike Module 2's `vat_rules`, which earned that because VAT rate changes are a live regulatory event; risk thresholds at pilot stage are not); the catalog's "RiskCase" is treated as the same thing Phase B already built — a risk indicator's own review lifecycle — not a second parallel aggregate. Also closed a latent bug: the NamRA portal's "Risk indicators" metric already filtered on `status === "OPEN"`, but before Phase B's seed correction no indicator ever had that value | CONTROLLED FOUNDATION | Officer-editable risk-rule proposals (if ever required), approved indicators/models, adverse-action review, appeal and model governance |
| 22 | Document | Private R2-backed upload, SHA-256 integrity, quarantine metadata, owner scope and no download before clean scan. A 2026-08-27 security audit found the MIME check trusted only the client-supplied `Content-Type` — a `.exe` declared `application/pdf` passed straight through. `validateAndHashFile` now sniffs the file's own leading bytes against real PDF/PNG/JPEG/XLSX magic numbers (CSV, having no signature, fails a bounded NUL-byte sniff instead), refusing a mismatched upload before it's ever stored; fixed the same day, and this is content-sniffing, not malware scanning — that gap (the row's own "Approved malware scanner" note below) remains. A 2026-08-26 code audit found every uploaded document stayed `status='QUARANTINED'`, `scan_status='PENDING_EXTERNAL_SCANNER'` forever — no command anywhere ever transitioned it, despite the row's prior maturity label implying a working quarantine lifecycle. Module 6 Phase A closed that gap. `CompleteDocumentScan` (`POST /api/v1/documents/:id/scan-result`) records an external scanner's verdict — `CLEAN` moves the document to `ACTIVE` (available), `INFECTED` to `REJECTED` (permanently blocked; the object is never deleted, so a rejected upload remains its own evidence) — restricted to a new `documents:manage` permission held only by `PILOT_ADMIN`/`NAMRA_SYSTEM_ADMIN`, the same officer-only posture `invoices:cancel` already established, since this represents an external system's callback rather than a taxpayer action. `SupersedeDocument` (`POST /api/v1/documents/:id/supersession`) is the `Version` concept the playbook names: `document_metadata` rows are themselves the version chain via a new `supersedes_document_id` column — the same pattern `audit_evidence.previous_version_id`/`vat_return_versions.parent_version_id` already use in this schema, rather than a separate `Version` table. Only a clean `ACTIVE` document can be superseded (a status check that also makes the chain a strict linked list — a document can only be superseded once); the superseded row and its R2 object are never deleted, only flipped to `status='SUPERSEDED'`. `GetDocumentVersionHistory` (`GET /api/v1/documents/:id/versions`) walks that chain in both directions so calling it on any version returns the complete history, oldest first. Module 6 Phase B closed the two remaining named gaps. `AuthorizedDownload` (`GET /api/v1/documents/:id/download`) now exists — previously there was no way to retrieve an uploaded document at all, since the route only ever exported `POST`. It refuses anything not currently `ACTIVE` or `SUPERSEDED` (both passed a clean scan at some point; `QUARANTINED` hasn't been scanned yet and `REJECTED` is permanently blocked), genuinely enforcing the "no download before clean scan" principle this row already claimed before the route existed to back it, and logs every access through the same audit-events hash chain as every other command. `ApplyRetentionHold`/`ReleaseRetentionHold` (`POST /api/v1/documents/:id/retention-hold`) give `legal_hold` a direct toggle — previously the only path was indirectly through Module 4's evidence-custody `SET_LEGAL_HOLD`/`RELEASE_LEGAL_HOLD` action, and only once a document had already been cited as audit evidence. The new path cascades the other way: applying or releasing a hold directly on a document also updates any `audit_evidence` rows that cite it, so the two hold paths can never disagree. The playbook's stronger claim — hold "checked by every deletion/retention job repo-wide" — remains unbuilt by design: a full-repo audit confirmed no deletion or retention/purge job exists anywhere in this codebase to wire it into, so there is nothing yet to enforce against | VERIFIED QUARANTINE | Approved malware scanner (today's scan verdict is manually asserted by an authorised admin, not an automated integration), a real deletion/retention job for the hold flag to guard once one exists, and DLP |
| 23 | Communication | A 2026-08-26 code audit found `communications` was read-only — a full-repo grep found zero `INSERT INTO communications` anywhere, despite this row's prior "records" claim. Module 6 Phase C built the write path. `SendNotice` (`POST /api/v1/communications/notices`) opens a new correspondence thread for a `CaseReference` (`AUDIT_CASE`/`REFUND_CLAIM`/`RECONCILIATION_EXCEPTION` — the same generic shape `related_resource_type`/`related_resource_id` already gave the table before this phase, now genuinely exercised); `taxpayer_id` is always derived from the referenced case, never caller-supplied, so a notice can never be misdirected. A new `communication_threads` table backs the `Conversation` concept — `communications.status` already meant something else (delivery status; the seed row is `'DELIVERED'`), so it couldn't double as a thread's open/closed state, making `Conversation` a genuinely new table rather than a reused column, unlike Phase A/B's `Version`/`RetentionHold`. `Respond` (`POST /api/v1/communications/:id/responses`) replies within an existing `OPEN` thread — reachable by either the NamRA side (`communications:manage`) or the taxpayer side (a new `communications:respond` permission, held by `TAXPAYER_OWNER`/`ADMIN`/`ACCOUNTANT`), with direction derived from the actor, never caller-supplied. `CloseConversation` (`POST /api/v1/communications/:id/closure`) is officer-only. `GetInbox` (`GET /api/v1/communications`) lists threads — not raw messages — each with a latest-message preview, filterable by status/case-reference-type, tenant-scoped, bounded/paginated with a real `total_count`; a companion `GET /api/v1/communications/:id` reads one thread's full message history | CONTROLLED FOUNDATION | Templates, a real delivery-provider integration (today's `channel`/`status` are recorded but nothing actually sends email/SMS/portal push), service policy and records-retention approval |
| 24 | Notification | A 2026-08-26 code audit found `notifications` was already real — five call sites across `openAuditCase`/`fileDispute`/`createObligation`/`approveRiskAction`'s case escalation/this module's own Phase C `sendNotice`/`respondToConversation` all wrote to it, each hand-rolling a near-identical `INSERT`, two as bare positional `VALUES` with no column list at all. Module 6 Phase D consolidated all five into one shared `notificationRecord` helper (the three new columns this phase added, `cancelled_by`/`cancelled_at`/`cancellation_reason`, would otherwise have silently broken those two bare-positional inserts). `Queue` (`POST /api/v1/notifications`) is the new standalone command a caller can reach directly — officer-only, matching every existing trigger of a notification in this codebase. A new `notification_preferences` table backs `UpdatePreference` (`POST /api/v1/notifications/preferences`, self-service, upserts); `Queue` checks it before attempting any channel beyond `IN_APP` when addressed to a specific `user_id` (a taxpayer-wide notification has no single user's preference to check, so it always attempts every requested channel). A new `notification_deliveries` table backs `DeliveryAttempt`, one row per channel actually attempted, always `status='QUEUED'` — no real provider exists, so nothing claims a false `'SENT'`. `CancelNotification` and a `MarkRead` action (not separately named by the playbook, but `read_at` was previously never written by anything anywhere) both exist, scoped to the actor who could see the notification in the first place. `GetNotifications` (`GET /api/v1/notifications`) is a dedicated, filterable, paginated read of the current actor's own notifications, replacing the bundled fixed 100-row projection inside `getComplianceSnapshot`. `TemplateVersion` deliberately not built — no template concept exists anywhere else in this schema to version, and every existing notification's title/message is composed inline by the command that triggers it | CONTROLLED FOUNDATION | Approved email/SMS/push providers (today's channels are recorded as queued, never actually delivered), consent rules and delivery receipts |
| 25 | Integration | A full-repo grep before Module 10 Phase A (2026-08-27) found `integration_connections`/`sync_jobs` had zero application write paths anywhere — every row was seed-only, despite this row's prior "VERIFIED FOUNDATION" claim implying a working registry. `RegisterIntegration`/`ApproveIntegration`/`SuspendIntegration` (`POST /api/v1/integrations`, `.../:id/approval`, `.../:id/suspension`) and `StartSync`/`GetHealth` (`POST`/`GET /api/v1/integrations/:id/sync`, `.../health`) now exist (`lib/data/integration-repository.ts`), provider-agnostic, built against a real `DRAFT → CONFIGURED ⇄ SUSPENDED` state machine (`lib/domain/integration.ts`). The four pre-seeded platform connections (ITAS/BIPA/bank-org1/treasury) carry free-text `REQUIRES_*_CONTRACT` status values that fall outside this closed enum, and `RegisterIntegration` refuses to create a duplicate `provider_key` in the same scope — so there is no path for these new commands to ever approve one of those genuinely external-gated rows live. Tenant actors manage only their own organisation's connections; actors with no taxpayer identity (national or platform-technical roles) manage platform-wide ones; neither can reach the other's. `StartSync` is honest that no live per-provider connector exists for *any* provider yet — every attempt against an approved connection completes immediately `FAILED` with a typed reason, never a fabricated success; `GetHealth` reads back the connection's own stored status and recent sync attempts, not a live probe | CONTROLLED FOUNDATION | Per-provider contracts, credentials, conformance sandbox, mTLS/signatures, DLP review, and an actual live per-provider connector implementation (today's `StartSync` proves the command shape only) |
| 26 | SaaS | Confirmed genuinely greenfield before Module 10 Phase C (2026-08-27) — no `saas_providers`/`saas_applications`/`saas_conformance_runs`/`saas_environment_approvals` tables existed anywhere. `RegisterProvider` (`POST /api/v1/saas-providers`) now creates a `SaaSProvider` and its first `Application` atomically (`lib/data/saas-repository.ts`); `SubmitConformance` (`POST /api/v1/saas-applications/:id/conformance-runs`) runs a real, fixed, code-versioned check harness (`lib/domain/saas.ts`) — `CAPABILITY_SCOPE_MATCHED`, `EVENT_CONTRACT_ACKNOWLEDGED` (checked against the literal `08-enterprise-architecture/event-catalog.csv` catalogue), `DATA_CLASSIFICATION_BOUNDED`, `SANDBOX_PRECEDES_PRODUCTION` — rather than trusting a self-reported pass/fail. A PASSED SANDBOX run grants real `EnvironmentApproval`; a PASSED PRODUCTION run deliberately still only reaches `AWAITING_AUTHORITY`, never `GRANTED` — no path exists for a self-submitted, self-run conformance suite alone to grant real production access. `GetUsage` (`GET /api/v1/saas-providers/:id/usage`) ties back into Module 10 Phase A's own `integration_connections`/`sync_jobs`, surfacing real usage for a vetted provider's `provider_key` across every tenant | CONTROLLED FOUNDATION | A real commercial/security operating model, human review of a passed PRODUCTION conformance run (today structurally unreachable — no command anywhere can grant it), and linking a vetted `SaaSProvider` to Phase A's own `RegisterIntegration` (today two independent, unlinked actions) |
| 27 | Developer platform | A full-repo grep before Module 10 Phase D (2026-08-27) confirmed `api_clients` (seed-only, `PENDING_CREDENTIAL_PROVISIONING`) had zero write paths anywhere, despite this row's prior "VERIFIED FOUNDATION" claim. `CreateClient` (`POST /api/v1/developer/clients`) now get-or-creates a `DeveloperAccount` (new `developer_accounts` table) and issues a real `client_key` immediately, while honestly keeping `status` at `PENDING_CREDENTIAL_PROVISIONING` — no external secret manager is integrated in this environment, and `credential_reference` is always a pointer string, never a secret value. `RotateCredential`/`RevokeCredential` (`.../rotation`, `.../revocation`) manage an explicit `CredentialRef` history (new `credential_refs` table); revocation is terminal, requires a reason. `RunConformance` (`.../conformance-runs`) runs a real, fixed check harness (`lib/domain/developer.ts`) against the client's current state — genuinely PASSable today, since it checks this platform's own internal bookkeeping, with one deliberately non-blocking `NOT_CONFIGURED` check naming the missing secret manager honestly rather than silently passing it | CONTROLLED FOUNDATION | External secret manager integration (today's credential_reference is always a pointer, never a live secret), gateway-level scope/rate-limit enforcement (today's scopes/rate_limit_profile are declared and validated but not yet enforced against live traffic — no API gateway exists in this deployment), and webhook delivery (webhook_subscriptions/webhook_deliveries remain seed-only, out of this phase's scope) |
| 28 | Reporting | A 2026-08-26 code audit found this row's prior "VERIFIED PILOT" claim overstated `report_definitions.audience`'s 2 pseudo-values (`'BOTH'`/`'NAMRA'`) as the 6-tier audience/product/freshness matrix `08-enterprise-architecture/22-audit-refund-reporting.md` actually specifies, and found any report code other than `VAT_POSITION`/`COMPLIANCE_CASELOAD` — including the already-seeded `SALES_VAT_SUMMARY` — silently fell through to a generic invoices-summary query rather than running its own implementation. Module 7 Phase A closed both gaps. `report_definitions` gained `freshness_tier` and `guardrail` columns; `audience` now holds the doc's real 6 tier codes (`TAXPAYER`, `PRACTITIONER`, `NAMRA_OPERATIONS`, `EXECUTIVE`, `AUDITOR_LEGAL`, `OPEN_DATA`). A new `requireAudienceAccess` guardrail dispatch runs before every report: `TAXPAYER` keeps the existing own-organisation-or-national scoping; `NAMRA_OPERATIONS` requires national scope; `EXECUTIVE` requires national scope plus a new `reports:executive` permission (`PILOT_ADMIN`/`NAMRA_SUPERVISOR` only); `AUDITOR_LEGAL` requires `audit:read` or `cases:manage` (closing a related gap: `NAMRA_AUDITOR` previously held neither `reports:read` nor `reports:run` at all); `PRACTITIONER` requires a live, `ACTIVE` row in the existing `delegations` table, scoping the report to exactly the taxpayers the actor is actually delegated for; `OPEN_DATA`'s guardrail (minimum-cell suppression) is enforced in the report's own result-shaping rather than as an access check. Four new reports were added — one per previously-unimplemented tier (`PORTFOLIO_EXCEPTIONS`, `REVENUE_COMPLIANCE_TRENDS`, `CASE_EVIDENCE_SUMMARY`, `NATIONAL_VAT_AGGREGATE`) — so the matrix is genuinely six working tiers, not six labels with only two ever exercised. Every `runInlineReport` response now states its `audience`/`freshness_tier`/`guardrail` alongside the result, a first step toward Phase C's fuller envelope. A definition with no matching code branch now fails closed (501) instead of silently substituting an unrelated report's output. Module 7 Phase B built the `Job`/`ExportManifest` concepts the playbook names, deliberately reshaping them for an environment with zero queue/cron infrastructure (confirmed via `wrangler.json`'s empty `queues`/`triggers`): a new `report_exports` table is the `ExportManifest`, but the export file itself is generated synchronously inside `RequestExport` (`POST /api/v1/reports/runs/:id/exports`), not dispatched to a background `Job`. The approval gate reuses Module 6's `document_metadata` `QUARANTINED`→`ACTIVE`/`REJECTED` lifecycle rather than duplicating it: exporting a `TAX_CONFIDENTIAL`/`RESTRICTED` report requires a fresh MFA step-up to request and starts `QUARANTINED` (not downloadable) until a second, different actor calls `ApproveExport` (`POST /api/v1/reports/exports/:id/approval`, maker-checker — self-approval is refused, and approving also requires a fresh step-up); anything less sensitive is auto-approved (`ACTIVE`) with no human gate. `CancelReport` (`POST /api/v1/reports/exports/:id/cancellation`) withdraws a still-pending export, flipping its document to `REJECTED`. `AuthorizedDownload` for exports (`GET /api/v1/reports/exports/:id/download`) is its own function, not a reuse of Module 6's `downloadDocument`, because it additionally gates on `report_exports.status='APPROVED'` and a new `report_exports.expires_at` (7 days from request) — deliberately not `document_metadata.retained_until`, which means "keep at least until," the opposite of an export's "available until." The exported file embeds a literal, testable watermark (`issued_to:<user> at:<timestamp> correlation:<id>`) as a CSV comment header rather than a visual/PDF stamp, and is capped at 200KB. Real encryption at the application layer was deliberately not built this phase — deferred pending a KMS/HSM authority, the same "fail closed on unconfirmed authority" posture this codebase already applies to invoice signing certificates; R2's own infrastructure-level encryption-at-rest is the only encryption property genuinely claimed today. Module 7 Phase C built the shared response envelope the playbook names — `as_of`, `audience`/`freshness_tier`/`guardrail` (carried over from Phase A), `filters` (the caller's own validated parameters), `currency_basis` (a real `"NAD"` constant, not a stub — every monetary figure anywhere in this schema is NAD-denominated, a genuinely single-currency pilot today), and `rule_version` (`report_definitions.query_version`, the report's own computation-logic version, not Module 2's VAT rule version, since most reports here have no VAT-rule dependency to version) — now wraps every `runInlineReport`/`publishReportRun` response instead of being three loose top-level fields. `PublishReport` (`POST /api/v1/reports/runs/:id/publication`, `publishReportRun`) is the "reconciliation-to-source-control-totals as a hard publication gate" the playbook names, built as a genuine live re-derivation rather than a second warehouse this pilot doesn't have yet: the same per-code query logic (`runInlineReport`'s branches, extracted into a shared `computeReportResult`) is re-run against current source data — using the run's own persisted `scope_snapshot`, not the publishing actor's scope, since e.g. a PRACTITIONER report's delegated-taxpayer set was resolved once at run time — and compared to the stored `result_summary`; a mismatch (a new invoice certified, a return superseded, a case closed since the run completed) refuses publication (409) until a fresh run is taken. A run can only be published once (idempotent-guarded, `report_runs` gained `published_by`/`published_at`/`scope_snapshot` columns), and a published run remains exportable through Phase B's export path (`RequestExport` accepts `COMPLETED_INLINE` or `PUBLISHED`) | VERIFIED PILOT | A real background job/queue for export generation (today's "asynchronous" export is computed synchronously inline, since no queue/cron infrastructure exists to defer it onto), application-layer encryption pending KMS/HSM authority, content-aware DLP scanning beyond the size cap, an open-data tier genuinely reachable without authentication, a governed control-totals ledger independent of the live query path (today's reconciliation re-derives from the same source tables the report itself reads, catching drift but not a corrupted query), and large-data performance evidence |
| 29 | Analytics | A 2026-08-26 audit found this row genuinely greenfield — nothing beyond the "ARCHITECTURE ONLY" label existed anywhere in this codebase. Module 7 Phase D built it. This deployment has no separate governed read replica/warehouse (the same Cloudflare D1 binding backs both the live fiscal write path and every read), so `PublishDataProduct`'s "never the live fiscal write store" requirement is enforced the strongest way actually available: `RunModel` (`POST /api/v1/analytics/data-products/:id/model-runs`) may only be fed by an already-`PUBLISHED`, already-reconciled report run (Module 7 Phase C's own reconciliation gate) — a genuinely verifiable claim, since `runAnalyticsModel`/`publishDataProduct` only ever read `report_runs`/`report_definitions`/`data_products`/`metrics`/`analytics_model_runs`/`data_product_snapshots`, never a fiscal source table directly. Mirrors Phase C's own `RunReport`→`PublishReport` split: `RunModel` computes a `ModelRun` (a new `analytics_model_runs` row, `model_output` a copy of the source run's already-certified `result_summary` — a genuine trend/forecast model is out of scope for this foundation phase, deliberately not faked); `PublishDataProduct` (`POST /api/v1/analytics/data-products/:id/publications`) promotes a completed `ModelRun` to the `DataProduct`'s current `data_product_snapshots` row (chained via `previous_snapshot_id`, mirroring Module 6's document-version chain), and refuses a source run whose `result_summary.suppressed===true` (Phase A's minimum-cell suppression carried through). `DataProduct`/`Metric`/`Lineage` definitions are seed-only, the same posture Phase A already established for `ReportDefinition` — defining a new governed metric is a config/governance action out of scope for this pilot. Every publish checks each `CERTIFIED` `Metric` on the data product against its previous snapshot value; a percentage change at or beyond the metric's own `anomaly_threshold_pct` raises a genuine, explainable `AnomalyCandidate` — persisted as a queryable `analytics_anomaly_candidates` row (`GET /api/v1/analytics/anomalies`), not just a fire-and-forget event, matching Module 4's "always return the explainable factor" precedent for risk indicators; `AnalyticsRefreshed` fires on every publish unconditionally. `QueryApprovedMetrics` (`GET /api/v1/analytics/metrics`, filterable by `data_product_id`/`code`) reads only certified metric definitions and the latest snapshot — never a live table either. `GET /api/v1/analytics/data-products` lists every data product with its lineage, certified metrics and latest snapshot in one read | CONTROLLED FOUNDATION | A real trend/forecast model (today's `ModelRun` re-packages an already-certified report figure, not a statistical model), a genuinely independent read replica/warehouse (today's boundary is "may only source from an already-published, reconciled report run on the same D1 binding," not physical isolation), a governed workflow for defining new DataProducts/Metrics (seed-only today, the same posture Reporting's own definitions carry), and privacy/model governance review |
| 30 | Administration | Separate NamRA Admin and Super Admin policies, service-component health. A 2026-08-26 audit found the "finance-data exclusion from technical admin" half of this row's claim was true only at the Next.js page level (`app/integrations/page.tsx` branched on the actor's role before calling the shared platform snapshot) — the `GET /api/v1/platform` route itself had no such branch, gated only by `platform:read`, which `SUPER_ADMIN`/`INFRASTRUCTURE_ADMIN` also hold. Not an active leak (those roles carry no organisation/taxpayer scope, so the scoped query returned nothing in practice), but incidental, not structural. Module 8 Phase A made it structural: `handlePlatformList` now routes a technical-only actor straight to `getTechnicalPlatformSnapshot()`, never reaching a query that touches `payment_instructions`/`bank_imports` at all. The same audit also found `FeatureFlag`/`PlatformConfig`/`AccessPolicy`/`ChangeRequest` were zero hits anywhere in code despite this row's "VERIFIED FOUNDATION" label — Phase A built all four. Definitions (which flags/config keys/policies exist) are seed-only, the same posture already established for `ReportDefinition`/`DataProduct` — a new governed knob existing at all is a deploy-time decision, not a runtime command; only an existing definition's *value* is runtime-changeable, and only through a real maker-checker gate. `RequestPlatformChange` (`POST /api/v1/platform/change-requests`, one generic command behind `ChangeFeature`/`ChangePolicy`/`ChangeConfig` rather than three near-identical copies) stages a proposed value as a `PENDING` `change_requests` row, snapshotting the previous value; `DecidePlatformChange` (`POST /api/v1/platform/change-requests/:id/decision`) applies or rejects it, refusing the requester's own decision. `GET /api/v1/platform/config` is the `GetConfig` read. `ProvisionStaff` (`POST /api/v1/platform/staff`, step-up gated) closes a real, distinct gap from Organisation Administration's `inviteEmployee` (Module 1, tenant-scoped): before this phase, a platform/NamRA technical staff account (no organisation, a national-scope role) could only come into existence via a hardcoded `db/runtime.ts` seed row — there was no command anywhere to create one | VERIFIED FOUNDATION | Central configuration/change APIs wiring into the modules whose hardcoded constants these seeded config values today only document (step-up window, export size cap, minimum-cell suppression threshold), PAM/JIT controls and independent SoD testing |
| 31 | Security | Bounded ingestion, layered rate limits, correlated logs/events, append-only audit, secret scan and SBOM were already real; "incidents" was not — a 2026-08-26 audit found `security_incidents` had zero `INSERT`/`UPDATE` anywhere in the codebase, a permanently-empty table silently rendering "no incidents" on `/security` regardless of actual security posture. Module 8 Phase B closed that gap for real. `Detection` is a small, fixed, code-versioned rule catalogue (`security_detection_rules`, seed-only definitions — the same posture Module 4's risk rules and Module 7's certified metrics already established) evaluated inline every time `recordSecurityEvent` is called, not by a polling job (this deployment has no cron/queue infrastructure, the same recurring gap Module 3's `RunMatch` and this module's own Phase A/C already documented). A rule fires once its `event_type`/actor-or-source grouping crosses `threshold_count` within `window_minutes`, opening an `OPEN` `security_incidents` row and de-duplicating against any already-open incident for the same rule+group so repeated events past the threshold don't spawn duplicates. `CreateIncident` (`POST /api/v1/security/incidents`) is the manual counterpart. `Contain` (`POST .../containment`) is a triage bookkeeping step (`OPEN`→`CONTAINED`); `Revoke` (`POST .../revocation`, step-up gated) is the genuine technical containment action — it revokes every `ACTIVE` `identity_links` row for the incident's subject user, reusing Module 1's own session-revocation mechanism rather than duplicating it, and is independently callable (not just after `Contain`) since a fresh session could appear later. `Close` (`POST .../closure`) is terminal, reachable directly from `OPEN` (a false positive) or from `CONTAINED`, requiring resolution notes. `GetSOCQueue` (`GET /api/v1/security/incidents`, filterable by `status`/`severity`) and a per-incident detail read (with its full `security_playbook_actions` history — a new table giving `PlaybookAction` genuine persistence, not just an audit-log mention) round out the read surface. A new `security:manage` permission gates the four write commands, held by `SUPER_ADMIN`/`INFRASTRUCTURE_ADMIN`/`SECURITY_ANALYST`/`PILOT_ADMIN` alongside their existing `security:read`. A 2026-08-27 audit found the detection rules' actual *input* coverage was much thinner than the catalogue implied: `AUTHORISATION_DENIED` was never recorded from the identity/control-plane/reconciliation/vat-rules route families (~60 routes), and `RATE_LIMIT_EXCEEDED` was missing from every already-partially-wired dispatcher's rate-limit branch — so `REPEATED_AUTHORISATION_DENIALS`/`RATE_LIMIT_ABUSE` structurally could not fire across most of the API, no matter how real the rule engine itself was. Fixed the same day with two shared, best-effort recorders (`lib/security/request.ts`'s `recordAuthorizationDenial`/`recordRateLimitBreach`) wired into every route family's shared error-mapping helper, proven end-to-end in `tests/routes/security-event-emission.test.ts` (a denial on a previously-blind route now genuinely opens a `security_incidents` row through the unmodified detection pipeline, not just an isolated event write) | VERIFIED FOUNDATION | Production IAM/PAM, KMS/HSM, WAF/SIEM integration (today's detections are this codebase's own fixed rule catalogue against its own `security_events`, not a SIEM feed), penetration test, continuous SCA and response exercises |

## Workspace, licensing and workflow extension coverage

Five domains from the workspace/organisation/licensing/workflow extension (`08-enterprise-architecture/30-workspace-organisation-licensing-workflow-architecture.md`) have working schema and logic in the repository (migration `0008`) but were not previously tracked as their own matrix rows. Recorded here for the first time; Workflow itself is tracked under domain #8's closure work (Module 8 in `MODULE_DEVELOPMENT_PLAYBOOK.md`), not repeated below.

| Domain | Current implementation | Status | Required closure before national production |
|---|---|---|---|
| Licensing & Entitlements | Plan/feature/entitlement/subscription/organisation-licence/usage schema, licence-continuity policy (`evaluateEntitlement`) wired into every control-plane command, standalone `GetEntitlements`/`GetUsage` queries (`GET /api/v1/licensing/entitlements`, `GET /api/v1/licensing/usage`), and standalone `Activate`/`Suspend`/`Renew` (`POST /api/v1/licensing/state`) plus `Upgrade` (`POST /api/v1/licensing/upgrade`, versioned plan-change history, not an in-place mutation) — the previously-dead `license_events` table now records every transition | VERIFIED PILOT | Commercial/billing provider integration; formal approval of the state-transition and plan-tiering rules |
| Organisation Administration | Employee/position/department/business-unit/administrator schema; invite, activate (`POST .../employees/:id/activation`, converts the USER_SEATS reservation into usage) and terminate-employee commands with licence-seat and workflow-reassignment effects; `AppointAdministrator` (`POST /api/v1/organisations/administrators`, requires an active employee record, demotes any existing primary when appointing a new one) | VERIFIED PILOT | NamRA/legal confirmation of administrator-proofing policy |
| Organisation Authorization | Versioned tenant-defined roles, role-permission grants, user-role and user-capability assignment schema; role creation with a protected-permission ceiling; standalone `GrantCapability` (`GET`/`POST /api/v1/organisations/capabilities`, upserts against the unique per-user-per-capability row, requires the organisation itself to already hold the capability and the target to be an active member) | VERIFIED PILOT | Formal governance approval of the protected-permission ceiling |
| Access Governance | Request/approve/review/certify schema and commands with self-approval and segregation-of-duties guards; quarterly review cycle with auto-close on full certification; standalone `RevokeAccess` (`POST /api/v1/access-grants/revocation`, revokes one active role or capability grant on demand) and `Offboard` (`POST /api/v1/organisations/offboarding`, revokes every active grant plus the membership itself, immediately — distinct from `terminateEmployee`'s licence/employment-coupled offboarding and from `certifyQuarterlyAccess`'s review-gated bulk revoke) | VERIFIED PILOT | Formal approval of the access-certification policy |
| Workspace & Navigation | Workspace/folder/item/preference schema; permission, feature and capability-filtered navigation query (`GetWorkspace`); standalone `GetChildren` (`GET /api/v1/navigation/children`, properly traverses `navigation_folders.parent_folder_id` unlike the flat `GetWorkspace` query), `GetActions` (`GET /api/v1/navigation/actions`, explainable single-item access check) and `SavePreference` (`POST /api/v1/navigation/preferences`, upserts) | VERIFIED PILOT | The `navigation_permissions` table exists but is not yet wired into the navigation query and should be connected or removed |

## Cross-cutting architecture evidence

| Concern | Repository evidence | Completion boundary |
|---|---|---|
| Tenant isolation | Organisation/taxpayer predicates in repositories, permission checks in page/API handlers, national-scope separation | Independent penetration and policy-bypass testing remains mandatory |
| Data integrity | Integer cents/quantity micros, uniqueness constraints, idempotency records, immutable correction lineage and hash-chained audit. Module 8 Phase D found this "hash-chained audit" claim was real in practice (117+ call sites across 8 repository files genuinely wired the audit trail as a command sink) but as 8 independently hand-rolled, near-identical copies of the same hashing logic — exactly the drift risk this module's own Phase C watch-outs already flagged for Workflow — plus one hand-inlined duplicate in `identity-repository.ts` using a different (non-canonical) JSON serializer than its own file's helper. All 9 sites now delegate to one shared `appendAuditEvent` (`lib/data/audit-repository.ts`); `business-repository.ts`'s own two-stage design (20+ call sites built around it) is a deliberate, documented exception, not a missed file. `GetAuditTrail` (`GET /api/v1/audit`, filterable by resource/action/actor, paginated) and `VerifyAuditChain` (`POST /api/v1/audit/chain-verifications`, on-demand — no cron infrastructure exists to schedule it — persists its own PASSED/FAILED result and opens a CRITICAL incident through Module 8 Phase B's own detection pipeline on a genuine tamper/corruption break) close the two real gaps the architecture doc named: previously only a Next.js page (`app/audit/page.tsx`) read the chain, and no verification of any kind existed anywhere in the codebase | Production database selection, migration rehearsal and independent reconciliation |
| Reliable integration | Transactional outbox, event catalogue, retry-safe commands, webhook/sync delivery state | Managed broker/relay, consumer replay tests and contracted providers |
| Offline safety | Device/range/batch/conflict models; untrusted submissions are rejected with explicit trust failure | Legal offline authority, enrolled device client, signed ranges and field pilot |
| Availability and scale | Health endpoints, rate/fairness controls, capacity/HA/DR manifests, SLO and playbooks | Funded topology, 2x-peak tests, failover and isolated restore exercises |
| Supply chain | Lockfile, lint/type/test/build gate, local secret heuristic and CycloneDX SBOM | Enterprise SAST/SCA, signed provenance, artifact signing and promotion controls |
| Sites data bindings | Structured state uses D1; private evidence blobs use R2 quarantine | Hosted environment binding, residency, backup and key-control approval |

## Verified release evidence

The canonical release gate passed on 2026-08-25:

- ESLint and TypeScript completed without errors.
- 222 unit/security/policy/route tests passed across twenty test files. `tests/routes/module-3-compliance-obligations.test.ts` proves CreateObligation is national-scope-only, idempotent on retry and rejects a duplicate taxpayer/type/period under a different key, MarkSatisfied is idempotent both under the same key and under a fresh key against an already-satisfied obligation, and GetComplianceCentre reflects newly created obligations. `tests/routes/module-3-reconciliation.test.ts` proves RunMatch is officer-only, matches a cleanly certified invoice and is idempotent on retry, and — by directly corrupting a ledger row after a clean submission to simulate drift/tampering — proves it correctly raises an EXCEPTION, which Assign and ResolveException (idempotent) then carry through to resolution. The same file's GetWorkQueue suite seeds deterministic fixture exceptions of controlled severity/status/assignment/age and proves tenant scoping (a taxpayer-side actor never sees another taxpayer's exceptions; a national-scope NamRA actor sees all), every filter predicate, and pagination with a genuine `totalCount`. `tests/routes/module-1-access-control.test.ts` proves cross-organisation access, cross-role access, expired step-up and insufficient assurance level are provably rejected end-to-end through real route handlers — the first tests in this repo to exercise anything beyond pure domain functions. `tests/routes/module-2-vat-rule-engine.test.ts` proves the VAT rule engine's fail-closed enforcement end-to-end. `tests/routes/module-2-invoice-hardening.test.ts` fires the same invoice submission twice via `Promise.all` and proves it never double-posts (a genuine interleaved-async concurrency test, not a sequential one), and proves the P0 unidentified-buyer guarantee directly at the ledger level. `tests/routes/module-2-invoice-lifecycle.test.ts` proves invoice_number collisions are rejected, CancelInvoice's officer-only/narrow-eligibility lifecycle, and that VerifyInvoice's public output surfaces correction and cancellation lineage. `tests/routes/module-2-transaction-timeline.test.ts` proves GetTransactionTimeline returns the right chronological, correctly-linked events for a plain certification, a certification-plus-correction lineage reachable from either invoice id, and a certification-plus-cancellation lineage. All run against a real SQLite engine (Node's built-in `node:sqlite`, wrapped to the `D1Database` shape) via test-only stand-ins for `cloudflare:workers` and `next/headers` (`tests/fakes/`, `tests/support/fake-d1.ts`) — necessary because both only resolve inside their real runtimes, which is why no route or repository code could be tested end-to-end before now.
- Heuristic secret scan passed and a CycloneDX SBOM was generated.
- The production build completed and exposed all application and API routes, including the new `GET /api/v1/me/access`, `POST /api/v1/registration-applications/:id/decision`, `POST /api/v1/organisations/:id/memberships`, `POST /api/v1/taxpayers/:id/suspension`, `GET`/`POST /api/v1/organisations/:id/branches` (+ `PATCH .../branches/:branchId`), `GET /api/v1/portals`, `GET /api/v1/counterparties/classification`, `GET /api/v1/licensing/entitlements`, `GET /api/v1/licensing/usage`, `POST /api/v1/licensing/state`, `POST /api/v1/licensing/upgrade`, `POST /api/v1/organisations/employees/:id/activation`, `GET`/`POST /api/v1/organisations/administrators`, `GET`/`POST /api/v1/organisations/capabilities`, `GET /api/v1/navigation/children`, `GET /api/v1/navigation/actions`, `POST /api/v1/navigation/preferences`, `GET`/`POST /api/v1/identity/links`, `GET /api/v1/identity/assurance`, `POST /api/v1/identity/links/:id/revocation`, `POST /api/v1/organisations/:id/invitations`, `POST /api/v1/invitations/claim`, `POST /api/v1/taxpayers/:id/identifiers/:identifierId/correction`, `POST /api/v1/taxpayers/:id/identifiers/verification`, `POST /api/v1/users/:id/suspension`, `POST /api/v1/users/:id/reactivation`, `POST /api/v1/access-grants/revocation`, `POST /api/v1/organisations/offboarding`, `GET`/`POST /api/v1/vat-rules`, `POST /api/v1/vat-rules/:id/approval`, `GET /api/v1/vat-rules/evaluate`, `GET /api/v1/invoices/:id/vat-explanation`, `POST /api/v1/invoices/:id/cancellation`, `GET /api/v1/invoices/:id/transaction-timeline`, `POST /api/v1/invoices/:id/match`, `POST /api/v1/exceptions/:id/assignment`, `POST /api/v1/exceptions/:id/resolution`, `GET /api/v1/exceptions`, `POST /api/v1/obligations` and `POST /api/v1/obligations/:id/satisfaction`.
- Runtime proof converted one accepted quotation to one certified invoice, linked the source quotation, created seller/buyer VAT ledger entries and returned the same invoice on an identical retry.
- Earlier runtime proofs covered versioned return workflows, blocked unconfigured ITAS submission, refund controls, invoice correction lineage, R2 quarantine, offline trust rejection, reports and separated portals.

## Production decision

The **approved implementable pilot scope is built and verified**. The system is **not authorised for statutory or national production**. Production approval remains blocked by the signed decisions and objective evidence listed in `08-enterprise-architecture/29-architecture-approval-gate.md` and `07-security-resilience/10-production-acceptance-gates.md`. No code change can legitimately substitute for those authorities, external contracts or infrastructure exercises.
