catalog_version: 1.0.0
envelope:
  specification: CloudEvents 1.0
  required_attributes: [specversion, id, source, type, subject, time, datacontenttype, dataschema, data]
  delivery: at-least-once
  ordering: guaranteed only within the declared aggregate partition
  idempotency: consumers persist event id and business-effect key
  compatibility: additive changes only within a major event version
events:
  - type: na.vatmsa.counterparty.verification-requested.v1
    owner: Business Party Trust Domain
    partition_key: organisation_id
    contains_tax_confidential_data: true
    purpose: Starts or restarts fail-closed counterparty verification after creation or an identity-field change without making the party transaction-eligible.
    minimum_payload: [business_party_id, trust_profile_id, status, correlation_id]
    controls: [atomic_outbox, minimized_payload, no_raw_provider_evidence, identity_change_reverification]
  - type: na.vatmsa.counterparty.trust-evaluated.v1
    owner: Business Party Trust Domain
    partition_key: organisation_id
    contains_tax_confidential_data: true
    purpose: Records a minimized current trust posture and explicit provider environment; synthetic outcomes never claim NamRA, ITAS or BIPA authority.
    minimum_payload: [business_party_id, trust_profile_id, trust_status, provider_environment, correlation_id]
    controls: [atomic_outbox, append_only_snapshot, freshness_expiry, synthetic_authority_separation]
  - type: na.vatmsa.identity.proofing-requested.v1
    owner: Identity Domain
    partition_key: proofing_case_id
    contains_tax_confidential_data: true
    purpose: Starts a durable proofing case in a non-authoritative pending posture; the event never provisions a taxpayer, organisation, membership, authorization or licence.
    minimum_payload: [proofing_case_id, registration_id, status, provider_environment, correlation_id]
    controls: [atomic_outbox, minimized_payload, provider_environment_label, no_raw_provider_evidence]
  - type: na.vatmsa.self-serve-signup.submitted.v1
    owner: Onboarding Domain
    partition_key: identity_subject_hash_or_contact_hash
    contains_tax_confidential_data: false
    purpose: Records a validated pending signup reference, requested placeholder plan and verification posture with no user, taxpayer, organisation, subscription, licence or payment activation effect.
  - type: na.vatmsa.license.state-changed.v1
    owner: Licensing and Entitlements Domain
    partition_key: organisation_id
    contains_tax_confidential_data: false
    purpose: Invalidates policy projections when an authorised licence authority changes state; restricted states preserve records and activate the approved continuity matrix.
  - type: na.vatmsa.license.entitlement-policy-changed.v1
    owner: Licensing and Entitlements Domain
    partition_key: organisation_id
    contains_tax_confidential_data: false
    purpose: Invalidates permission, navigation and command policy projections after an approved feature, limit or operation-class mapping changes.
  - type: na.vatmsa.license.operation-denied.v1
    owner: Licensing and Security Operations Domains
    partition_key: organisation_id
    contains_tax_confidential_data: false
    purpose: Records a minimized fail-closed denial code, feature, operation class and correlation reference without copying request bodies or taxpayer records.
  - type: na.vatmsa.quotation.converted.v1
    owner: Commercial and Invoice Domains
    partition_key: organisation_id
    contains_tax_confidential_data: true
    purpose: Links an accepted quotation to its independently certified fiscal invoice through recoverable idempotent processing.
  - type: na.vatmsa.invoice.corrected.v1
    owner: Invoice Domain
    partition_key: taxpayer_id
    contains_tax_confidential_data: true
    purpose: Links an independently certified credit or debit note to its original invoice with reason and signed VAT effect while retaining both histories.
    minimum_payload: [invoice_id, original_invoice_id, correction_type, certificate_id, tax_rule_set_id, tax_rule_set_version, certification_hash, correlation_id]
    controls: [atomic_outbox, approved_effective_rule_required, append_only_correction_link, no_full_document]
  - type: na.vatmsa.document.quarantined.v1
    owner: Evidence Custody Domain
    partition_key: taxpayer_id
    contains_tax_confidential_data: true
    purpose: Records the private object key, integrity checksum and pending external malware-scan state without making the object consumable.
  - type: na.vatmsa.offline.batch-rejected.v1
    owner: Offline Synchronisation Domain
    partition_key: device_id
    contains_tax_confidential_data: true
    purpose: Preserves an untrusted, replayed, out-of-sequence or unverifiable batch attempt and its explicit rejection reason.
  - type: na.vatmsa.report.completed-inline.v1
    owner: Reporting Domain
    partition_key: taxpayer_id
    contains_tax_confidential_data: true
    purpose: Records completion of a controlled tenant-scoped aggregate report without exposing its result payload to the event stream.
  - type: na.vatmsa.dispute.filed.v1
    owner: Dispute Domain
    partition_key: taxpayer_id
    contains_tax_confidential_data: true
    purpose: Records an immutable taxpayer challenge for independent review.
  - type: na.vatmsa.refund.requested.v1
    owner: Refund Domain
    partition_key: taxpayer_id
    contains_tax_confidential_data: true
    purpose: Starts staged evidence and risk review only after statutory filing eligibility.
  - type: na.vatmsa.refund.request-blocked.v1
    owner: Refund Domain
    partition_key: taxpayer_id
    contains_tax_confidential_data: true
    purpose: Records a negative position that cannot advance because the return is not acknowledged as filed.
  - type: na.vatmsa.refund.reviewed.v1
    owner: Refund Domain
    partition_key: taxpayer_id
    contains_tax_confidential_data: true
    purpose: Records one staged human review without creating a payment by itself.
  - type: na.vatmsa.return.approval-requested.v1
    owner: VAT Return Domain
    partition_key: taxpayer_id
    contains_tax_confidential_data: true
    purpose: Places an immutable return version into maker-checker approval.
  - type: na.vatmsa.return.approved.v1
    owner: VAT Return Domain
    partition_key: taxpayer_id
    contains_tax_confidential_data: true
    purpose: Records independent approval and locks the VAT period against ordinary writes.
  - type: na.vatmsa.return.rejected.v1
    owner: VAT Return Domain
    partition_key: taxpayer_id
    contains_tax_confidential_data: true
    purpose: Records independent rejection and reopens the period for a superseding version.
  - type: na.vatmsa.return.submission-blocked.v1
    owner: ITAS Integration Domain
    partition_key: taxpayer_id
    contains_tax_confidential_data: true
    purpose: Makes missing authority rules or provider configuration explicit without claiming legal filing.
  - type: na.vatmsa.adjustment.approval-requested.v1
    owner: VAT Return Domain
    partition_key: taxpayer_id
    contains_tax_confidential_data: true
    purpose: Records a proposed VAT adjustment that has no return effect until independently approved.
  - type: na.vatmsa.quotation.issued.v1
    owner: Commercial Domain
    partition_key: organisation_id
    contains_tax_confidential_data: true
    purpose: Announces a server-calculated quotation without treating it as a fiscal invoice.
  - type: na.vatmsa.quotation.accepted.v1
    owner: Commercial Domain
    partition_key: organisation_id
    contains_tax_confidential_data: true
    purpose: Records the authorised acceptance of an unexpired quotation.
  - type: na.vatmsa.quotation.edited.v1
    owner: Commercial Domain
    partition_key: organisation_id
    contains_tax_confidential_data: true
    purpose: Announces a new immutable hashed revision of an unexpired issued quotation.
  - type: na.vatmsa.quotation.rejected.v1
    owner: Commercial Domain
    partition_key: organisation_id
    contains_tax_confidential_data: true
    purpose: Records terminal rejection of an unexpired issued quotation while retaining its reason in protected audit evidence.
  - type: na.vatmsa.quotation.expired.v1
    owner: Commercial Domain
    partition_key: organisation_id
    contains_tax_confidential_data: true
    purpose: Records the explicit terminal expiry of an issued quotation after its valid-until date.
  - type: na.vatmsa.journal.posted.v1
    owner: Accounting Domain
    partition_key: organisation_id
    contains_tax_confidential_data: true
    purpose: Announces an immutable, balanced general-ledger journal.
  - type: na.vatmsa.expense.recorded.v1
    owner: Expense Domain
    partition_key: organisation_id
    contains_tax_confidential_data: true
    purpose: Records a tenant-scoped expense pending its evidence and approval workflow.
  - type: na.vatmsa.expense.receipt-linked.v1
    owner: Expense Domain
    partition_key: organisation_id
    contains_tax_confidential_data: true
    purpose: Records immutable linkage of a clean and available receipt with its integrity checksum to a draft expense.
  - type: na.vatmsa.expense.approved.v1
    owner: Expense Domain
    partition_key: organisation_id
    contains_tax_confidential_data: true
    purpose: Records independent approval of a draft expense without creating an accounting posting.
  - type: na.vatmsa.expense.rejected.v1
    owner: Expense Domain
    partition_key: organisation_id
    contains_tax_confidential_data: true
    purpose: Records independent rejection and preserves the protected decision reason.
  - type: na.vatmsa.inventory.movement-recorded.v1
    owner: Inventory Domain
    partition_key: organisation_id
    contains_tax_confidential_data: true
    purpose: Announces a signed stock movement after the non-negative balance invariant is satisfied.
  - type: na.vatmsa.project.created.v1
    owner: Project Domain
    partition_key: organisation_id
    contains_tax_confidential_data: true
    purpose: Creates a project control object and optional proposed budget.
  - type: na.vatmsa.invoice.received.v1
    owner: Invoice Domain
    partition_key: taxpayer_id
    contains_tax_confidential_data: true
    purpose: Confirms durable receipt before business validation.
  - type: na.vatmsa.invoice.rejected.v1
    owner: Invoice Domain
    partition_key: taxpayer_id
    contains_tax_confidential_data: true
    purpose: Records terminal validation failure and structured reasons.
  - type: na.vatmsa.invoice.certified.v1
    owner: Certification Domain
    partition_key: taxpayer_id
    contains_tax_confidential_data: true
    purpose: Announces signed certification after the invoice, authority-approved effective tax rule, certification hash and VAT transaction commit atomically.
    minimum_payload: [invoice_id, transaction_id, certificate_id, tax_rule_set_id, tax_rule_set_version, certification_hash, correlation_id]
    controls: [atomic_outbox, approved_effective_rule_required, certificate_rule_version_match, no_full_document]
  - type: na.vatmsa.vat.posted.v1
    owner: VAT Ledger Domain
    partition_key: taxpayer_id
    contains_tax_confidential_data: true
    purpose: Announces balanced VAT sub-ledger posting.
  - type: na.vatmsa.match.resolved.v1
    owner: Reconciliation Domain
    partition_key: transaction_id
    contains_tax_confidential_data: true
    purpose: Records the counterpart match decision, confidence and evidence.
  - type: na.vatmsa.exception.opened.v1
    owner: Exception Domain
    partition_key: taxpayer_id
    contains_tax_confidential_data: true
    purpose: Opens a controlled discrepancy workflow.
  - type: na.vatmsa.exception.resolved.v1
    owner: Exception Domain
    partition_key: taxpayer_id
    contains_tax_confidential_data: true
    purpose: Records disposition, evidence and authorised actor.
  - type: na.vatmsa.return.drafted.v1
    owner: Return Domain
    partition_key: taxpayer_period_id
    contains_tax_confidential_data: true
    purpose: Announces a reproducible return draft built from a closed ledger snapshot.
  - type: na.vatmsa.risk.alerted.v1
    owner: Risk Domain
    partition_key: taxpayer_id
    contains_tax_confidential_data: true
    purpose: Raises explainable risk indicators; does not itself impose an adverse decision.
  - type: na.vatmsa.audit.case-opened.v1
    owner: Audit Domain
    partition_key: taxpayer_id
    contains_tax_confidential_data: true
    purpose: Creates an investigation case with immutable evidence references.
  - type: na.vatmsa.offline.batch-received.v1
    owner: Offline Synchronisation Domain
    partition_key: device_id
    contains_tax_confidential_data: true
    purpose: Records batch sequence, device signature and chain-integrity result.
