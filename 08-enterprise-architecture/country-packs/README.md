# Architecture country-pack specifications

This directory contains non-executable reference manifests used to review the VAT-MSA country-pack contract. It is not a runtime package registry.

No manifest here is production enabled. Runtime implementation requires ADR approval, signed artifacts, validated schemas, a separate trusted registry and a passed Country Readiness Gate.

`NAM` is the first reference specification and remains `UNDER_REGULATORY_REVIEW`.
